# 🌿 YEOWOOL 플러그인 — 진행 상황 정리

> Paper 1.21.4 / Java 21 / Gradle(Kotlin DSL) 멀티모듈 / MySQL
> 10개 플러그인(`yeowool-core`, `economy`, `land`, `life`, `market`, `analytics`, `community`, `admin`, `teleport`, `proxy`)으로 구성

---

## 1. 프로젝트 구조

```
YEOWOOL_PLUGIN/
├─ yeowool-core        (모든 플러그인의 공통 기반)
├─ yeowool-economy     (온 경제)
├─ yeowool-land        (토지 시스템)
├─ yeowool-life        (농사/벌목/목축/낚시/채광/직업)
├─ yeowool-market      (NPC상점/플레이어상점/거래/경매)
├─ yeowool-analytics   (통계/랭킹)
├─ yeowool-community   (채팅/프로필/칭호/친구/이벤트/코스메틱)
├─ yeowool-admin       (운영 도구/모더레이션/쿠폰)
├─ yeowool-teleport    (홈/워프/tpa)
└─ yeowool-proxy       (Velocity, 서버 이동/대기열)
```

`./gradlew build`로 전체 빌드(테스트 포함), 각 모듈 `build/libs/*.jar`를 `plugins/`에 넣으면 됩니다.
Paper의 라이브러리 로더(`plugin.yml`의 `libraries:`)를 사용해서 HikariCP/MySQL 커넥터/Caffeine을 셰이딩 없이 런타임에 받아옵니다.
`gradle.properties`의 `junitVersion`으로 JUnit 5를 전 모듈에 공통 적용 — 순수 로직이 있는 모듈은 `src/test`에 단위 테스트가 있음 (18절 참고).

---

## 2. YeowoolCore — 공통 기반

- **데이터베이스**: HikariCP + MySQL, 시작 시 스키마 자동 생성/마이그레이션
- **플레이어 데이터**: 온(지갑), 은행 잔액, 토지 레벨/XP, 토지 목록, 설정, 통계 — 전부 캐시 + 비동기 저장
- **공통 API**(`YeowoolCoreAPI`): 다른 모든 플러그인이 `Bukkit.getServicesManager()`로 가져다 씀
  - `economyData()` — 잔액 변경은 전부 자동으로 로그에 남음 (`PlayerBalanceChangeEvent`도 발행)
  - `landStats()` — 레벨/XP 저장, XP 변경 시 `PlayerLandXpChangeEvent` 발행
  - `playerData()`, `messages()`, `sounds()`, `logs()`, `mailbox()`, `punishments()`, `dataSource()`
- **GUI 프레임워크**(`YeowoolGui`): 클릭 자동 취소 + 필요한 슬롯만 "편집 가능"으로 지정 가능 (거래/기본템 GUI에서 사용)
- **메시지 시스템**: MiniMessage 기반, `messages.yml`에서 플레이스홀더는 `<이름>` 문법 사용
- **우편함 시스템** (`/우편함`, `core.mailbox()`): 어떤 플러그인이든 아이템을 상대에게 바로 못 줄 때(오프라인, 인벤토리 가득 참) `deliverOrStore()`로 넘기면 자동으로 DB에 보관했다가, 다음 접속 시 자동 지급을 먼저 시도하고 그래도 안 들어가면 `/우편함` GUI에서 클릭해 수령. 경매 낙찰 아이템 지급 + `/여울관리 우편`(관리자 발송)에 적용됨
- **제재 시스템** (`core.punishments()`): 경고/추방/정지/음소거를 `yw_punishments`에 기록하는 공용 원장. 정지는 접속 시도 자체를 막고(`AsyncPlayerPreLoginEvent`), 음소거는 YeowoolCommunity의 채팅 리스너가 막음 — 명령어는 YeowoolAdmin 쪽 참고 (9절)
- **명령어 도움말** (`/여울도움말 [카테고리]`, 별칭 `/도움말`): 10개 플러그인에 흩어진 명령어를 카테고리별로 모아 보여주는 색인. config.yml의 `help.categories`에 사람이 직접 정리해둔 목록이라, 다른 플러그인에서 새 명령어를 추가하면 여기도 같이 업데이트해야 함 (자동 수집 아님)
- `/여울코어 reload`

---

## 3. YeowoolEconomy — 경제

- `/돈`, `/돈 보내기 <닉네임> <금액>` (오프라인 대상도 지원)
- `/은행`, `/은행 입금\|출금 <금액>`
- **Vault 연동**: Vault가 설치되어 있으면 자동으로 `Economy` 등록 (EssentialsX 등 다른 플러그인이 "온"을 인식하게 됨). 은행(bank) 계열 API는 미지원으로 응답 (Yeowool의 개인 은행과 개념이 달라서)

---

## 4. YeowoolLand — 토지 시스템

- **배럴 등록 흐름**: 빈 청크에 배럴 설치 → 토지 생성/확장. 레벨별 최대 청크 수는 데이터 기반 테이블(config.yml)
- **보호**: 블록 파괴/설치, 상자·문 등 상호작용, 폭발, 화재 확산, 동물/PVP 피해 — 비멤버는 전부 차단
- **한 번 설치된 배럴은 파괴 불가** (OP 또는 `yeowool.land.bypass` 권한 보유자는 예외)
- **스태프 보호 우회** (`yeowool.land.bypass`): 이 권한을 가진 스태프는 멤버가 아닌 남의 땅에서도 건축/상자·문 상호작용/동물 피해 제한을 전부 무시하고 그리핑 조사·복구를 할 수 있음
- **다른 사람 청크 진입 시** "{소유자}님의 청크" 타이틀 표시 (청크가 바뀔 때만 체크 → 성능 부담 없음)
- **토지 은행** (`/토지 은행 입금\|출금 <금액>`) — 개인 은행과 별개인 토지 전용 금고
- **토지 업그레이드**: XP를 채우면 "가능" 알림만 오고, `/토지 업그레이드`를 직접 실행해야 적용 (토지 은행에서 비용 차감, config로 레벨별 비용 설정)
- `/토지 확인` — 보유 청크 경계에 파티클 표시
- `/토지 정보`, `/토지 멤버`, `/토지 초대\|추방 <닉네임>`, `/토지 삭제`(본인 소유만, 그리핑 방지 위해 대상 지정 불가), `/토지 설정 pvp`
- **멤버 권한 세분화** (`/토지 권한 <닉네임> <건축|상자> <켜기|끄기>`) — 멤버별로 "건축만 가능"/"상자만 가능"을 따로 켜고 끌 수 있음 (기존엔 멤버=전체 권한이었음). 멤버 목록에 `이름(건축,상자)`처럼 현재 권한이 같이 표시됨
- **관리자 도구** (`/토지 관리 목록|삭제|이전`, `yeowool.land.bypass` 전용): 서버 전체 토지 목록 조회(소유자/청크수/레벨), 그리핑당한 토지 강제 삭제, 소유권 강제 이전(대상이 이미 다른 토지를 갖고 있으면 거부)
- 첫 접속 스타터 지급 로직은 **YeowoolAdmin으로 이전**됨
- **마을 이름** (`/토지 이름 [이름]`, 2~16자, 인자 없이 실행하면 해제): 토지에 이름을 붙일 수 있음. 이름을 정하지 않은 토지는 어디서나 "이름없는 마을"로 표시됨
- **마을 랭킹** (`/마을랭킹`): 소유자의 토지 레벨(동률 시 XP) 기준 TOP 10을 이름/소유자/레벨/멤버 수/청크 수와 함께 보여줌 — DB 조회 없이 메모리에 있는 데이터로 바로 계산
- **업그레이드 성장 보너스** (`land-level` 각 티어의 `reward`): `/토지 업그레이드` 성공 시(비용을 낸 후) 그 토지 은행으로 보너스 온을 지급 — 다 같이 쓰는 마을 자금이라는 느낌을 주기 위함. 기본값은 Lv.5에 3000, Lv.10에 10000 (0으로 두면 지급 안 함)

---

## 5. YeowoolLife — 생활 콘텐츠

- **농사**: 모든 바닐라 작물 성장시간 정확히 10분 고정 (서버 재시작에도 DB에 저장된 남은 시간으로 이어서 성장)
- **나무 묘목도 고정 시간 성장** (기본 15분, config 조정 가능) — 바닐라 랜덤 성장 대신. 정글/다크오크도 2x2 없이 혼자 심어도 자람 (바닐라 대비 의도적 편의 변경). 뼈가루로 즉시 완성은 그대로 허용
- **우클릭으로 남은 시간 확인**: 작물·묘목을 우클릭하면 그 위에 "남은 시간: X분 Y초" 텍스트가 3초간 떠 있음 (본인에게만 보임)
- **작물 밟기 방지**: 플레이어·동물이 경작지를 밟아도 흙으로 변하지 않음 (경작지가 흙으로 변하면서 위 작물이 부서지는 게 진짜 원인이었음)
- **경작지 마름 방지**: 실제로 성장 중인 작물 밑 경작지만 항상 수분 최대로 유지 (서버 전체를 훑지 않아 성능 부담 없음, 애초에 성장 속도는 고정 타이머라 수분과 무관하고 순전히 겉모습용)
- **벌목**: 플레이어가 심은 나무는 XP 제외 (자연 성장만 인정)
- **목축**: 청크당 개체수 제한, 번식 XP
- **낚시**: 등급(일반/희귀/특수/전설)별 룰렛으로 물고기 종을 뽑음
- **채광**: 바닐라 그대로 + XP/통계
- **사냥** (신규): 몬스터를 처치하면 XP + 통계 누적 (전투 자체는 바닐라 그대로, PVP 킬은 집계에서 제외). 몬스터별로 실제로 전투력에 영향을 주지는 않는 순수 수집 요소
- **`/도감`이 4개 카테고리 허브로 확장됨**: 물고기(기존)/광물/사냥/작물, 각각 등급 또는 종류별로 몇 개를 모았는지 GUI로 확인 (아직 못 모은 항목은 "???" 실루엣). 광물/사냥/작물 항목의 `id`는 실제 바닐라 Material/EntityType 이름과 정확히 일치해야 자동 집계됨 (채광/농사 리스너에 한 줄씩만 추가해서 기존 카운터 옆에 세부 통계를 같이 쌓는 방식이라 별도 이벤트 처리 부담 없음)
- **매크로 탐지 연동**: 낚시/채광/사냥 모두 할 때마다 `PlayerRepeatableActionEvent`를 발행 — YeowoolAdmin이 이 이벤트로 반복 간격을 분석함 (9절 참고)
- **ItemsAdder 커스텀 작물** (ItemsAdder 설치 시에만 활성화): 작물마다 원하는 개수의 성장 단계와, **단계별로 서로 다른 성장 시간** 설정 가능. 바닐라 10분 규칙과 완전히 분리된 시스템
- **직업 시스템** (`/직업 선택`, `/직업 정보`, `/직업 목록`, `/직업 스킬`): 목수·농부·어부·요리사·연금술사 중 **한 번에 하나만** 활성화 가능 (전직해도 이전 직업 레벨/경험치는 그대로 보존, 다시 선택하면 이어짐). 직업에 맞는 행동(나무 블록 설치/작물 수확/낚시/화로 요리 완성/양조기 물약 완성)을 하면 경험치를 얻고, 레벨업마다 스킬 포인트 지급
  - `/직업 목록` — 모든 직업의 설명·아이콘·(플레이해본 적 있다면) 현재 레벨/경험치를 보여주는 **정보용 GUI** (읽기 전용, 클릭해도 아무 일 없음)
  - `/직업 선택` — 직업을 고르는 **선택용 GUI**, 클릭하면 그 직업으로 즉시 전환 (`/직업 선택 <직업>`으로 이름을 직접 입력하는 기존 방식도 그대로 지원)
  - `/직업 스킬` — 모든 직업이 공유하는 **4개의 패시브 스킬**에 포인트 배분: 숙련 경험치(경험치 +%), 생산 마스터리(확률적 추가 획득 +%), 결정적 성과(경험치 2배 확률 +%), 부수입(확률적으로 고정 온 지급) — 액티브 스킬은 없음
  - 직업 아이콘/설명/최대 레벨/경험치 곡선/스킬 4종 전부 config.yml에서 조정 가능하지만, 실제 경험치 획득 트리거(어떤 행동이 어떤 직업 경험치를 주는지)는 5개 직업에 맞춰 코드로 구현됨 (config만으로 새 트리거 타입 추가는 안 됨)

---

## 6. YeowoolMarket — 상점 및 거래

- **NPC 상점**: 여러 개 만들 수 있음, 상점마다 크기/모드(구매만·판매만·둘다)/장식(도트 텍스처) 설정 가능. `/상점 [상점ID]`
- **NPC 상점 한정 판매(로테이션)**: 상점 config에 `rotation.pool`(후보 목록)/`rotation.slots`(로테이션 슬롯 위치)/`rotation.interval-minutes`(주기)를 설정하면, 주기마다 pool에서 슬롯 개수만큼 무작위로 뽑아 채움. 기존 고정 `items`와 별개로 동작하며 안 쓰면 예전처럼 고정 상점 그대로
- **플레이어 상점**: `/거래소 등록\|내판매\|취소`, 구매 시 3% 수수료 소각
- **직접 거래**: `/거래 <닉네임>` 요청 → `/거래 수락` → GUI에서 아이템·온 제안 → 양쪽 확인 시 동시 교환
- **Citizens 연동**: NPC 우클릭으로 지정한 상점 열기 (설치 시에만)
- **ItemsAdder 연동**: 상점 아이템을 커스텀 아이템(`item-id`)으로 등록 가능
- **경매장** (`/경매`, `/경매 등록 <시작가> <즉시구매가|0> <시간(분)>`, `/경매 검색 <키워드>`): 입찰은 즉시 에스크로(잔액 차감), 상회 입찰 시 이전 입찰자 자동 환불, 즉시구매가 설정 시 Shift+클릭으로 바로 낙찰. 시간이 지나면 자동으로 정산(수수료 차감 후 판매자 지급, 유찰 시 아이템 반환). 검색은 아이템 이름/타입으로 필터링
- **부정거래 탐지 (가격 조작)**: 플레이어 상점에 개당 단가가 비정상적으로 높은 판매글을 올리거나, 짧은 시간에 판매글을 몰아서 등록하면 온라인 관리자에게 실시간 경고 + 로그 기록 (config로 임계치 조정 가능)

---

## 7. YeowoolAnalytics — 통계

- `/여울통계` — 서버(TPS/접속자)/경제(총 온/평균 보유)/토지(토지 수/평균 레벨)/생활(누적 채집량) 리포트
- `/랭킹 [온|은행|토지레벨|낚시도감]` — 항목별 TOP 10 순위표 (권한 제한 없음, 모두 사용 가능)
- **시즌 랭킹 보상** (`/시즌랭킹 [종료]`, 종료는 `yeowool.analytics.season.manage` 전용): 매주(기본 `season.period-days: 7`, 매시간 자동 확인) 랭킹 TOP 3에게 온 보상(기본 100000/50000/30000) 지급 + 서버 전체 방송 후 초기화. 랭킹 기준은 `season.score`라는 새 통계값인데, 실제 온/토지 레벨 등 게임 데이터를 절대 건드리지 않기 위해 YeowoolLand의 토지 XP 획득 이벤트(농사/채광/낚시/사냥/번식 등에서 나오는 XP)를 그대로 누적한 것 — 시즌이 끝나면 이 값만 0으로 리셋되고 실제 온/레벨/XP는 전혀 영향 없음. 오프라인인 수상자도 `PlayerDataResolver`로 정상 지급됨. 마지막 리셋 시점은 DB가 아니라 플러그인 데이터 폴더의 `season-state.yml`에 저장

---

## 8. YeowoolCommunity — 커뮤니티

- **채팅 채널** (`/채널 전체|지역|마을`로 기본 채널 전환, `/전체`·`/지역`·`/마을 <메시지>`로 채널 안 바꾸고 한 번만 발송): 전체채팅(서버 전원), 지역채팅(반경 100블록, config로 조정 가능), 마을채팅(같은 토지 소속 멤버들에게만 — 별도 마을/길드 시스템 없이 기존 `/토지` 데이터를 그대로 재사용). 칭호 접두사 + 스팸 쿨다운 + 코스메틱 채팅색은 모든 채널에 공통 적용, 음소거 상태면 어떤 채널로도 전송 불가
- **귓속말** (`/귓속말` 또는 `/w`, `/msg`, `/tell` — 친구 여부와 무관하게 온라인 상태인 아무에게나 가능) + `/답장`으로 마지막으로 받은 귓속말에 바로 답장
- **프로필**: `/프로필 [닉네임]`
- **칭호+업적** (하나로 통합): 통계 기준 자동 해금 + 온 보상, `/칭호 목록|장착|해제`. 통계 조건이 없는(`requirement-stat` 미설정) 칭호는 자동 해금되지 않는 관리자 전용 칭호로 취급되며, `/칭호 지급|회수 <플레이어> <id>`(`yeowool.community.title.manage`)로 부여/회수 — 예시로 `staff`("[STAFF]") 칭호를 기본 설정에 넣어뒀음. 부여만으로는 표시되지 않고 대상이 `/칭호 장착`으로 직접 장착해야 함
- **`/칭호생성 <id> <표시>`** / **`/칭호삭제 <id>`** (`yeowool.community.title.manage`, OP 기본): 위와 같은 관리자 전용(자동 해금 조건 없는) 칭호를 게임 중에 새로 만들고 지울 수 있음. `config.yml`의 `titles`는 직접 건드리지 않고 별도 `created-titles.yml`에 저장함 — `FileConfiguration.save()`는 파일 전체를 다시 쓰면서 기존 주석을 전부 날려버리기 때문에, 주석이 많은 `config.yml`을 코드에서 직접 저장하지 않기 위한 선택. `TitleManager.reload()`가 `config.yml`과 `created-titles.yml` 양쪽을 합쳐서 불러옴. `/칭호삭제`는 `created-titles.yml`에 있는(즉 `/칭호생성`으로 만든) 칭호만 지울 수 있고, `config.yml`에 정의된 칭호는 파일을 직접 수정해야 함
- **PlaceholderAPI 확장**: `%yeowool_on%`(온), `%yeowool_bank%`(은행), `%yeowool_landlevel%`, `%yeowool_landxp%`, `%yeowool_playtime%`, `%yeowool_title%`(칭호), `%yeowool_online%`, `%yeowool_max_players%`, `%yeowool_tps%` — 다른 플러그인(채팅/스코어보드 플러그인 등)에서 그대로 사용 가능
- **사이드바 스코어보드 / 탭리스트**: 접속 중인 모든 플레이어에게 온/토지레벨/서버 정보를 실시간으로 보여주는 사이드바 표시, 탭리스트 헤더/푸터도 같은 정보로 갱신 (PlaceholderAPI 없이도 자체 플레이스홀더로 동작, 설치 시엔 외부 플레이스홀더도 함께 해석)
- **이벤트 시스템** (`/이벤트 시작|종료|정보|보상받기`): 관리자가 이름/배율/시간을 정해 시즌 이벤트를 시작하면 그 시간 동안 토지 XP 획득량에 배율이 적용되고, 종료 시(또는 참여자가 `보상받기`로) 설정된 보상 아이템 지급
- **미션 이벤트** (`/이벤트 미션시작 <처치|수집> <대상> <목표수> <분>`, `미션종료`, `미션정보`): "좀비 100마리 처치" "다이아몬드 50개 수집" 같은 서버 전체 목표를 걸고, 가장 먼저 달성한 한 명에게 보상 지급. XP 배율 이벤트와 별개로 동시에 진행 가능
- **미션 자동 스케줄링** (`mission.schedule`, `config.yml`): 관리자가 매번 수동으로 시작 안 해도, 설정한 시각(서버 시간, 예: 매일 20:00)에 자동으로 미션이 시작됨 (다른 미션이 이미 진행 중이면 건너뜀, 하루 한 번만 발동). `/이벤트 미션시작`으로 수동 진행하는 것도 그대로 가능
- **`/출석체크`** (GUI, 일일/주간/월간 3단): 일일은 연속 출석 일수를 추적해 7일/30일 등 마일스톤에 보너스 지급(`attendance.streak-bonus`), 주간/월간은 스트릭 없이 마지막 수령 후 N시간이 지나면 다시 받을 수 있는 독립 쿨다운(`attendance.weekly`/`attendance.monthly`). GUI 배경(인벤토리 제목에 폰트 이미지로 렌더링)과 아이콘은 사용자가 제공한 "Rewards UI by MCMobs" ItemsAdder 애드온(namespace `rewards`, 배경 5종 `rewards_blue/brown/green/purple/red`, 아이템 `rewards_daily/dailye/weekly/weeklye/monthly/monthlye`)을 그대로 사용 — 랭크 아이콘과 동일한 `FontImageWrapper.replaceFontImages` 기법. 그 애드온의 `contents/rewards` 폴더를 서버 ItemsAdder에 옮기고 `/iazip`해야 실제로 보이고, 없으면 자동으로 기본 종이 아이템 + 평범한 제목으로 대체됨 (원래 다운로드 받은 패키지에는 별도의 "DailyRewards"라는 서드파티 플러그인을 새로 설치하는 안내가 있었지만, 그 대신 이미 만들어둔 자체 출석 시스템에 이 애드온의 텍스처/배경만 재사용하는 쪽을 선택함)
- **`/퀘스트`** (일일/주간 2종): 왼쪽 버튼(시계 아이콘)이 일일 퀘스트, 오른쪽 버튼(해바라기 아이콘)이 주간 퀘스트 GUI를 엶. 새 이벤트 리스너 없이, YeowoolLife 등이 이미 쌓고 있는 통계 키(`life.mining.mined` 등)의 "이번 기간 동안의 증가량"을 재는 방식이라 어떤 행동을 새 퀘스트 종류로 추가해도 코드 수정 없이 `config.yml`의 `daily.quest-pool`/`weekly.quest-pool`만 늘리면 됨 (`QuestManager` 하나가 일일/주간 둘 다 처리 — 리셋 주기만 다름: 일일은 서버 로컬 날짜, 주간은 ISO 주차 기준). 출석과는 완전히 분리된 별도 시스템 (원래 하나의 `/데일리` 명령어였다가 왼쪽/오른쪽 버튼 요청에 맞춰 `/출석체크`+`/퀘스트`로 분리함)
- **코스메틱 상점** (`/코스메틱`): 파티클 트레일(예: 불꽃/하트/음표)과 채팅 색상 꾸미기를 온으로 구매해 장착. 칭호 보상과는 별개로 순수 장식용 상품군
- **친구 시스템** (`/친구 추가|수락|거절|삭제|목록`): 온라인 상대에게 요청(60초 후 만료) → 수락하면 친구 등록. 친구가 접속하면 알림, 목록에서 온라인/오프라인 표시
- **랭크 아이콘** (`/랭크아이콘 목록|부여 <닉네임> <id>|제거 <닉네임>`, 부여/제거는 `yeowool.community.rankicon.manage`): ItemsAdder의 `font_images`(`beautiful_ranks` 리소스팩) 기반 아이콘을 채팅/탭리스트/네임태그에 표시. 칭호·코스메틱과 달리 플레이어 스스로 해금하지 않는 관리자 전용 지급 방식. id는 리소스팩의 `:owner_icon:` 같은 font_images 키와 일치해야 함. 아무 아이콘도 없는 일반 플레이어는 `rank-icon-default-id`(기본 `player_icon`)가 자동 적용됨. 렌더링은 최종적으로 `FontImageWrapper.replaceFontImages(":id:")` (채팅 placeholder 치환 API)로 정착함 — 중간에 `new FontImageWrapper(id)` 직접 생성 + `exists()`/`getString()` 방식으로 바꿔봤다가 되돌림: 그 생성자는 팩에 없는 id를 넘기면 즉시 `IllegalArgumentException`을 던져서 매 틱마다 예외가 터지며 사이드바/탭리스트 갱신 루프 전체가 죽는 장애가 있었고, 예외를 다 잡아도 `getString()`의 출력 자체가 실제로는 렌더링되지 않았음(원래 `replaceFontImages`로는 보이던 아이콘이 이 방식으로 바꾸자 전부 안 보이게 됨) — `replaceFontImages`가 검증된 경로라 그대로 사용. 실패 감지는 `.equals(placeholder)`가 아니라 `.contains(placeholder)`로 판단함 (치환 실패 시 반환값이 원본과 완전히 동일하다는 보장이 없어서, `.equals()`로는 실패를 못 잡고 `:owner_icon:` 같은 원본 텍스트가 그대로 노출되는 버그가 있었음). `SidebarScoreboardTask`/`TablistTask`는 플레이어 한 명의 렌더링 실패가 나머지 전원에게 번지지 않도록 플레이어별 try-catch를 유지함 (안전망). 참고: 탭리스트의 개별 플레이어 이름은 헤더/푸터처럼 매 틱 갱신되는 게 아니라 접속/`/랭크아이콘`/`/칭호`/`/한글닉네임설정권` 실행 시점에만 `PlayerIdentityService.refresh()`로 갱신되므로, 이미 접속해 있던 플레이어의 탭리스트 표시가 안 바뀌면 재접속하거나 해당 명령어를 한 번 더 실행해서 강제로 갱신해야 함
- **한글 닉네임** (`/한글닉네임설정권 [닉네임]`, OP 전용, `yeowool.community.nickname.grant`): 설정권 아이템(ItemsAdder 커스텀 아이템, 미설치 시 기본 종이)을 우클릭하면 가상 모루가 열려 한글 닉네임(2~8자)을 입력 가능 — `/쿠폰`과 동일한 미리보기(PrepareAnvilEvent)/확정(결과 슬롯 클릭) 2단계 구조. 확정 시 설정권 1개 소모 후 저장하고, 채팅/탭리스트/네임태그에 실명 대신 표시. 아머스탠드 방식이 아니라 `Player#playerListName(Component)`로 표시 이름 자체를 갈아끼우는 방식 — 1.19.3+ 클라이언트에서는 이 값이 머리 위 네임태그에도 그대로 반영됨
- **표시 이름 통합** (`PlayerIdentityService`): 랭크 아이콘 + 칭호 + 한글 닉네임(또는 실명) 조합을 한 곳에서 계산해 채팅(`ChatChannelService`), 탭리스트의 플레이어 목록/네임태그(`playerListName`), 사이드바 스코어보드(`<identity>` 토큰) 세 곳 모두에 동일하게 적용 — 접속 시, 그리고 아이콘/칭호/닉네임이 바뀔 때마다 갱신. 예: 랭크 아이콘 없이 `staff` 칭호만 장착한 관리자는 탭리스트에 "[STAFF] 관리자"로 표시됨
- **접속 직후 사이드바/탭리스트 렌더링 경쟁 상태 수정**: `core.landStats().getLandLevel/getLandXp`는 해당 플레이어의 `PlayerData`가 아직 캐시에 로드되지 않았으면(접속 직후 비동기 로드가 끝나기 전) 예외를 던지는데, `PlaceholderTokens.build`가 이를 무조건 호출하고 있어서 막 접속한 플레이어의 사이드바/탭리스트 첫 갱신 몇 틱이 실패하는 경우가 있었음 (이전엔 조용히 그 틱만 실패하고 다음 틱에 저절로 복구돼서 안 보이던 문제였는데, 위 try-catch로 로그가 뜨면서 드러남). `PlayerData`가 아직 로드되지 않았으면 토지 레벨/경험치를 0으로 대체하도록 수정
- **탭리스트 헤더/푸터** (`tablist.header`/`footer`, `config.yml`): `<servername>` 토큰 추가 — 이 서버 인스턴스를 가리키는 이름(`server-name` 설정값, 기본 "야생")을 표시. 프록시로 여러 서버를 묶어 운영할 때 서버별 `config.yml`에 각각 다른 값을 넣으면 됨



---

## 9. YeowoolAdmin — 운영 도구

- `/여울관리 정보|지급|공지|백업|복구|기본템|로그|우편`
- **플레이어 모더레이션** (`/경고`, `/추방`, `/정지`, `/정지해제`, `/음소거`, `/음소거해제`, `/제재기록`): 전부 `yw_punishments`(YeowoolCore)에 기록됨. 정지는 접속 자체를 차단(사유/만료시각 표시), 음소거는 채팅을 막음. 기간은 `30m`/`2h`/`7d`/`영구` 형식, `/제재기록 <닉네임>`으로 한 플레이어의 경고/추방/정지/음소거 이력을 전부 확인 가능
- **플레이어 신고** (`/신고 <닉네임> <사유>`): 권한 제한 없이 누구나 사용 가능(60초 쿨다운), 온라인 관리자에게 실시간 알림 + 로그 기록
- **관리자 우편 발송** (`/여울관리 우편 <닉네임> [메모]`): 손에 든 아이템을 그대로 대상의 우편함으로 전송 (온라인이면 바로 인벤토리, 아니면 우편함에 보관)
- **기본템 GUI** (`/여울관리 기본템`): GUI에 아이템을 놓고 닫으면 그대로 초반 지급템으로 저장 (관리자가 직접 설정하기 전까진 아무것도 지급 안 됨)
- **아이템 도구**:
  - `/귀속` — 손에 든 아이템 귀속 토글 (일반 유저는 버리기 불가, OP는 항상 가능)
  - `/기간제 설정 <기간>` (예: `30m`, `2h`, `1d`) / `/기간제 해제` — 일반 유저가 실제로 소지를 시작한 시점부터 카운트다운. 설명 맨 아래에 `7일제`처럼 자동으로 표시되고(`@` 기호는 제거함), `/아이템설명 변경`으로 설명을 바꿔도 이 태그는 다시 붙여져서 사라지지 않음. 기존에 만들어진 `@7일제` 형식 아이템도 재설정하면 새 형식으로 자동 교체됨
  - `/아이템이름 변경 <이름>`, `/아이템설명 변경 <설명>` — MiniMessage 서식(굵게/기울임/색상) 전부 지원, 설명은 `\n` 또는 `|`로 줄바꿈
  - `/아이템 복사 <개수>` — 손에 든 아이템을 복제해 인벤토리로
  - `/아이템내구도 무한` — 손에 든 아이템 내구도 무한 토글
- **관리자 아이템 카탈로그** (`/관리자아이템`): GUI에서 클릭 한 번으로 지급 (기본값: 토지 배럴)
- **로그 조회 GUI** (`/여울관리 로그 <닉네임> [개수(기본 200, 최대 500)]`): 모든 플러그인이 `yw_logs`에 쌓아온 기록(잔액 변경, 부정거래 탐지, 토지 권한 변경 등)을 GUI로 조회 (원래는 채팅에 한 번에 최대 100줄 텍스트로 뿌리는 방식이었음). 대상의 로그를 한 번에 전부 불러온 뒤 카테고리 필터(나침반 아이콘 클릭 시 다음 카테고리로 순환)와 페이지 넘김을 전부 메모리에서 처리 — 필터를 바꾸거나 페이지를 넘겨도 DB를 다시 조회하지 않음. `LogViewerGui` (`yeowool-admin/logviewer`)
- **부정거래 탐지**:
  - 잔액 급증 — 비정상적으로 큰 잔액 증가를 온라인 관리자에게 실시간 경고 + 로그 기록
  - 아이템 복제 — 30초(설정 가능)마다 접속 중인 플레이어의 인벤토리/엔더상자를 훑어서 바닐라에서는 나올 수 없는 "최대 스택 수 초과" 아이템을 찾아 정상 수치로 즉시 정정하고 관리자에게 경고 (복제 버그·변조 클라이언트 탐지)
  - 상점 가격 조작 — YeowoolMarket 쪽에 구현 (6절 참고): 개당 단가 이상치, 짧은 시간 판매글 폭탄 등록 탐지
  - 매크로/자동화 의심 — 낚시·채광처럼 반복 가능한 행동의 최근 20회 간격을 통계적으로 분석해, 사람이라면 나오기 힘들 정도로 일정한 간격(표준편차 60ms 미만)이 감지되면 경고 (완전한 안티치트는 아니고 스태프가 확인할 신호를 주는 수준)
- **백업**: 수동/자동 CSV 백업. 자동 백업이 실패하면 콘솔 로그뿐 아니라 온라인 관리자에게도 즉시 알림
- **백업 복구** (`/여울관리 복구 <백업폴더명>`, `yeowool.admin.super` 전용): 지정한 백업 폴더의 CSV를 다시 읽어 각 테이블에 `REPLACE INTO`로 되돌림 (기본키 기준 덮어쓰기라 통째로 비우지 않음). 폴더명은 백업이 만든 `yyyyMMdd-HHmmss` 형식만 허용(경로 조작 방지), 탭완성으로 존재하는 백업 목록이 뜸
- **업데이트 공지**: 접속 시 표시 (버전 비교로 중복 방지)
- **쿠폰 시스템**: 유저는 **`/쿠폰`을 입력하면 실제 모루 블록 없이도 모루 GUI가 바로 열리고**, 이름 입력창에 쿠폰 이름을 적으면 오른쪽 결과 슬롯에 보상 아이템이 미리보기로 뜸 — **그 아이템을 클릭해야 실제로 지급**되고 GUI가 자동으로 닫힘 (타이핑만으로 바로 지급되지 않도록 미리보기/확정 2단계로 분리 — 이름을 입력하는 즉시 지급하던 초기 버전은 클라이언트가 같은 입력에 대해 서버 이벤트를 두 번 보내는 경우가 있어 "사용했습니다"와 "이미 사용한 쿠폰입니다"가 동시에 뜨는 버그가 있었음, 클릭으로 확정하는 방식으로 바꿔 해결). GUI를 채우기 위해 넣어둔 "여기에 쿠폰 이름을 입력하세요" 종이 아이템은 인벤토리로 새지 않고 닫을 때 자동 폐기됨. 실제 모루 블록에서도 똑같이 작동하므로 `/쿠폰` 없이 직접 모루를 써도 됨. 관리진 명령어:
  - `/쿠폰생성 <이름> <만료일>` — 명령어 실행 시 **주손에 든 아이템**이 보상으로 등록됨. 만료일은 `2026-08-23`처럼 **날짜 형식**으로 입력 (그 날짜가 끝날 때까지 유효)
  - `/쿠폰관리 목록` — 전체 쿠폰과 만료 여부 확인
  - `/쿠폰관리 <이름> 정보` — 보상/생성일/만료일 확인
  - `/쿠폰관리 <이름> 제거` — 쿠폰과 사용 기록을 함께 삭제
  - `/쿠폰관리 <이름> 수정 아이템` — 보상 아이템을 주손에 든 아이템으로 교체
  - `/쿠폰관리 <이름> 수정 기간 <만료일>` — 만료일을 날짜 형식으로 재설정

  같은 쿠폰은 플레이어당 1회만 사용 가능하고, 만료/이미 사용한 쿠폰 이름을 입력하면 안내 메시지만 뜨고 정상적인 모루 개명은 그대로 가능 (일반 개명 기능을 방해하지 않음)

---

## 10. YeowoolTeleport — 순간이동

`/홈`, `/워프`, `/tpa` 세 명령어군 전부 하나의 `TeleportService`를 통해 같은 규칙(이동 전 대기시간, 이동 중 취소, 쿨다운)을 공유합니다.

- **홈** (`/홈 [이름]`, `/홈 설정|삭제|목록 [이름]`): 플레이어당 최대 개수(`teleport.max-homes`, 기본 3개) 제한. 이름 생략 시 "home"이라는 기본 이름 사용
- **워프** (`/워프 [이름]`, `/워프 설정|삭제 <이름>`): 서버 전체가 공유하는 관리자 등록 지점. 설정/삭제는 `yeowool.teleport.warp.manage` 권한 필요 (기존 `yeowool.admin`과 분리 — 건축팀 등에 별도로 위임 가능하도록). 이름 없이 `/워프` 또는 `/워프 목록`을 치면 **GUI**로 전체 워프를 보여주고 클릭해서 바로 이동 가능 — 빈 슬롯 장식/워프 아이콘 둘 다 config.yml의 `warp-gui`에서 재질과 `custom-model-data`(도트 텍스처 리소스팩용)를 지정 가능, NPC 상점의 decoration 설정과 같은 방식
- **tpa** (`/tpa <닉네임>` 상대에게 이동, `/tpahere <닉네임>` 상대를 내게 부름, `/tpaccept`, `/tpdeny`): 60초 후 자동 만료
- **안전장치**: 이동 전 기본 3초 대기(`teleport.delay-seconds`), 대기 중 한 블록이라도 움직이거나 피해를 입으면 자동 취소(전투 중 도주 방지), 이동 성공 후 5초 쿨다운(`teleport.cooldown-seconds`) — 전부 config로 조정 가능

---

## 11. YeowoolProxy — Velocity

- `/서버 <이름>` — 서버 이동, 점검 서버 목록, 인원 초과 시 대기열

---

## 12. 전체 명령어 탭완성

모든 명령어에 서브커맨드·닉네임 자동완성 적용됨 (`/토지`, `/돈`, `/은행`, `/상점`, `/거래소`, `/거래`, `/경매`, `/프로필`, `/칭호`, `/이벤트`, `/코스메틱`, `/친구`, `/랭킹`, `/채널`, `/귓속말`, `/직업`, `/홈`, `/워프`, `/여울관리`, `/여울도움말`, `/귀속`, `/기간제`, `/아이템`, `/아이템내구도`, `/아이템이름`, `/아이템설명`, `/경고`, `/추방`, `/정지`, `/음소거`, `/제재기록`, `/쿠폰`, `/쿠폰관리` 등).

---

## 13. 외부 플러그인 연동

| 플러그인 | 상태 | 없을 때 |
|---|---|---|
| **Vault** | 연동 코드 있음 (softdepend) | 여울 경제는 정상 작동, 타 플러그인만 온 인식 못함 |
| **Citizens** | 연동 코드 있음 (softdepend) | `/상점` 명령어로만 이용 |
| **ItemsAdder** | 연동 코드 있음 (softdepend) | 바닐라 작물/아이템만 |
| **ModelEngine** | 연동 코드 없음(공개 저장소가 없는 유료 플러그인) | Citizens NPC에 자체 명령어로 모델만 입히면 됨, 저희 코드 관여 X |
| **PlaceholderAPI** | 연동 코드 있음 (softdepend) | 여울 자체 스코어보드/탭리스트는 정상 작동, 외부 플러그인이 `%yeowool_*%`만 인식 못함 |
| **LuckPerms** | 그룹 설계 완료 | `luckperms-setup.txt` 스크립트로 총관리진/관리진/가이드/유저 4단계 그룹 + 권한 노드를 콘솔에 붙여넣기만 하면 적용됨. **주의**: `yeowool.*` 권한 노드가 전부 `default: op`라 서버 op 계정은 그룹 설정과 무관하게 전부 허용됨 — 실질적인 등급 분리를 하려면 총관리진 외 스태프는 deop 필요 |

---

## 14. 겪었던 버그와 원인

1. **MySQL 연결 실패로 전체 플러그인 연쇄 비활성화** — Core가 죽으면 의존하는 모든 플러그인이 `NoClassDefFoundError`로 실패하는 정상적인 연쇄 반응. MySQL 서버 설치로 해결.
2. **`/돈` 등에서 플레이스홀더가 `{on}`처럼 안 바뀌고 그대로 표시** — MiniMessage는 플레이스홀더를 `<이름>` 문법으로 써야 하는데 `{이름}`로 잘못 작성했던 것. 전체 messages.yml 수정 완료.
3. **`/상점`에서 "설정된 상점이 없습니다"** — 코드 버그가 아니라 상점 구조를 다중 상점 방식으로 바꾸면서 서버에 이미 생성된 구버전 `config.yml`이 안 맞게 된 것. 해당 설정 파일을 지우고 재생성해야 함.
4. **`ALTER TABLE ... ADD COLUMN IF NOT EXISTS` SQL 오류** — 이 문법은 MySQL 8.0.29 이상에서만 지원됨. `INFORMATION_SCHEMA` 조회로 컬럼 존재 여부를 직접 확인하도록 수정해 모든 MySQL/MariaDB 버전에서 동작하도록 고침.
5. **`messages.yml` 수정이 서버에 반영 안 됨** — `saveResource`는 파일이 이미 있으면 덮어쓰지 않기 때문에, 구조/문구가 바뀐 리소스 파일은 서버에서 수동으로 지워야 최신 버전으로 재생성됨. (config.yml/messages.yml 구조가 바뀔 때마다 반복될 수 있는 이슈이니 참고)

> **참고**: 4번·5번처럼, config.yml이나 messages.yml의 *구조 자체*가 바뀌는 업데이트를 받을 때마다 서버에 이미 생성된 옛날 파일이 안 맞을 수 있습니다. 플러그인을 새로 받은 뒤 이상하게 동작하면 가장 먼저 의심할 것은 "이 플러그인의 config.yml/messages.yml을 지우고 재시작"입니다.

---

## 15. 범위에서 제외한 것 / 후속 과제로 남긴 것

- ~~토지 보호 스태프 우회 권한 없음~~ → **해결**: `yeowool.land.bypass` 권한 추가 (2절/4절 참고)
- ~~경매 미수령 아이템 우편함 없음~~ → **해결**: 우편함 시스템 추가 (2절 참고), 경매 낙찰 지급에 바로 적용됨

---

## 16. 확인이 필요한 가정

- `/토지 삭제`는 **본인 소유 토지만** 삭제하도록 구현했습니다 (닉네임으로 남의 토지를 삭제 못하게). 다른 의도였다면 알려주세요.
- 토지 업그레이드 비용(`land-level.cost`)은 기본값으로 임의 설정했습니다 — 밸런스는 실제 플레이하면서 조정 필요.
- 작물/묘목 우클릭 시 남은 시간 표시는 **고정 시간 성장**으로 확정 (요청 시 결정).
- 경매 수수료 5%, 판매글 단가 이상치 기준 1억 온, 판매글 폭탄 기준 60초에 10건 — 전부 config 기본값이라 실제 경제 규모에 맞게 조정 필요.
- **모더레이션 명령어는 전부 기존 `yeowool.admin` 권한을 재사용**했습니다 (경고/추방/정지/음소거 등에 별도 세분화 권한을 만들지 않음). 신고/랭킹/친구는 권한 제한 없이 전원 사용 가능, 미션 이벤트는 기존 `yeowool.event.manage` 재사용, 토지 관리자 도구는 기존 `yeowool.land.bypass` 재사용 — **새로 추가된 권한 노드는 없어서 `luckperms-setup.txt`를 다시 손볼 필요 없습니다.**
- 매크로 탐지 임계치(표준편차 60ms, 최근 20회 샘플, 경고 쿨다운 5분)는 초기값이라 오탐/미탐 비율을 보면서 조정 필요.
- 미션 이벤트 보상은 "가장 먼저 달성한 한 명"에게만 지급하도록 구현했습니다 (참여자 전원 지급이 아님). 다른 의도였다면 알려주세요.
- **오프라인 대상 지원 여부를 전수 점검**했습니다 — 결과: 텔레포트(tpa)/귓속말/친구/거래는 애초에 상대가 실시간으로 온라인이어야 의미가 있는 기능이라 온라인 전용으로 그대로 뒀고(설계상 의도), `/돈 보내기`와 `/여울관리`의 대상 지정 명령어들은 이미 전부터 오프라인을 지원하고 있었습니다. 이번에 새로 지원 대상에 추가한 건 랭크아이콘/칭호/한글닉네임뿐입니다.
- **신규 시스템들의 보상 액수**(일일 퀘스트 2000온, 주간 퀘스트 8000온, 출석 1000온+연속보너스, 시즌 랭킹 100000/50000/30000온, 마을 레벨업 보너스 3000/10000온)는 실사용 트래픽 데이터가 없어서 정밀하게 튜닝하지는 않았고, 기존에 이미 설정되어 있던 보상들(칭호 3000~10000, 이벤트 5000, 미션 20000, 코스메틱 15000~30000)과 상대적 크기가 어색하지 않도록만 맞췄습니다. 실제 서버 경제 규모를 보고 조정해주세요.
- 채팅 시스템은 확인 후 결정된 대로 구현: 지역채팅 반경 100블록(`chat.local-radius`로 조정 가능), 마을채팅은 별도 마을/길드 시스템 없이 기존 토지 멤버십을 그대로 사용, 귓속말은 친구 여부와 무관하게 온라인 상태인 아무에게나 가능.
- 직업 시스템도 확인 후 결정된 대로 구현: 동시에 하나의 직업만 활성화(여러 직업 동시 진행 아님), 스킬 포인트는 액티브 스킬 없이 패시브 보너스(경험치 +%, 추가 획득 확률 +%)에만 사용. 요리사는 화로/훈제기/모닥불에서 "완성된 음식을 꺼낼 때", 연금술사는 양조기에서 "완성된 물약을 꺼낼 때"를 경험치 획득 시점으로 잡았습니다 (실제로 누가 재료를 넣었는지까지는 추적하지 않음 — 대부분의 유사 플러그인이 쓰는 근사 방식).
- 쿠폰 시스템의 `/쿠폰관리 <이름> 수정`은 무엇을 바꾸는지 명시하도록 `수정 아이템` / `수정 기간 <기간>`으로 하위 인자를 나눴습니다 (원래 요청엔 세부 인자가 없었음). 쿠폰 보상은 `/쿠폰생성` 실행 시 주손에 든 아이템 하나만 지원합니다 (온 지급이나 여러 아이템 조합은 아직 없음).
- **한글 닉네임의 머리 위 네임태그**는 "아머스탠드 방식 금지"라는 요청에 따라 `Player#playerListName(Component)`(공식 Paper API, 탭리스트용) 하나로 채팅/탭리스트/네임태그를 전부 처리했습니다. 1.19.3+ 클라이언트는 이 값을 머리 위 네임태그에도 그대로 반영하는 것으로 알고 있으나, 실제 서버 클라이언트 버전에서 반드시 실측 확인이 필요합니다 — 만약 네임태그에 실명이 계속 보인다면 스코어보드 팀 접두사(실명 앞에 덧붙이는 방식, 완전 대체는 아님) 또는 ProtocolLib 같은 패킷 라이브러리 추가가 필요합니다(후자는 새 필수 의존성이라 별도로 알려주시면 진행하겠습니다).
- 랭크 아이콘 id 목록(`config.yml`의 `rank-icons`)은 `beautiful_ranks.yml`에 있는 `_icon` 접미사 키들로만 채워뒀습니다 (기본 `owner`/`admin`처럼 `_icon`이 없는 버전도 있음) — 실제로 어떤 아이콘을 쓸지는 목록을 열어보고 조정해주세요.
- 한글 닉네임 설정권의 기본 아이템(`korean-nickname.voucher-item-id`)은 말씀하신 `killeffect_golden_scroll.png`가 들어있는 다른 애드온(`ec_killeffects:kfx-scroll-killeffects_golden_scroll`)의 네임스페이스 ID를 그대로 넣어뒀습니다 — 실제 서버 리소스팩에 이 애드온을 포함/병합한 뒤 ID가 다르면 `config.yml`에서 바꿔주세요. ItemsAdder가 없거나 ID를 못 찾으면 자동으로 기본 종이 아이템으로 대체됩니다.
- **설정 파일 자동 병합 (`ConfigMerger`, YeowoolCore)**: `saveDefaultConfig()`/`saveResource(false)`는 서버에 이미 파일이 있으면 새 버전에서 추가된 키를 절대 반영하지 않아서, 매번 기능을 추가할 때마다 `messages.yml`이 "[missing message: ...]"로 뜨거나 `config.yml`의 새 항목이 비어 보이는 문제가 있었습니다. 이제 모든 플러그인이 시작 시 부족한 키만 골라서 채워 넣도록 바꿨습니다 (기존에 직접 수정한 값은 그대로 유지, 완전히 새로운 키만 추가). **단, 이미 존재하는 키의 "기본값"이 바뀐 경우는(예: 사이드바 `<player>` → `<identity>`) 자동으로 덮어쓰지 않으므로 서버의 실제 `config.yml`/`messages.yml`을 직접 열어서 비교/수정해야 합니다.**

---

## 17. LuckPerms 권한 그룹 (총관리진 > 관리진 > 가이드 > 유저)

`luckperms-setup.txt` 파일로 별도 전달했습니다 — 서버 콘솔에 그대로 붙여넣으면 4단계 그룹과 권한이 한 번에 세팅됩니다.

| 그룹 | 상속 | 추가되는 권한 | 역할 |
|---|---|---|---|
| **유저** | (기본, LuckPerms `default` 그룹 그대로 사용) | 없음 (대부분의 플레이어 명령어는 애초에 권한 노드가 없어 전원 사용 가능) | 일반 플레이어 |
| **가이드** | 유저 | `yeowool.admin.alerts`, `yeowool.analytics.view` | 부정거래 경고 관전 + 통계 열람만 가능한 신뢰 도우미. 아이템/경제 조작 명령어는 없음 |
| **관리진** | 가이드 | `yeowool.admin`, `yeowool.core.admin`, `yeowool.event.manage`, `yeowool.teleport.warp.manage` | 통합 관리 명령어, 아이템 도구, 백업(생성), 이벤트 운영, 워프 등록까지 가능한 일반 운영진 |
| **총관리진** | 관리진 | `yeowool.admin.super`, `yeowool.land.bypass` | 백업 **복구**, 남의 땅 보호 무시 등 되돌리기 어렵거나 강력한 명령어까지 전부 가능한 최고 관리자 |

**주의할 점**: 지금까지 만든 모든 `yeowool.*` 권한 노드는 `plugin.yml`에 `default: op`로 선언돼 있어서, 바닐라 서버 op를 가진 계정은 이 그룹 설정과 무관하게 전부 통과됩니다. 즉 이 4단계로 실제 등급을 나누려면 **총관리진을 제외한 나머지는 서버 op에서 빼고(deop) LuckPerms 그룹으로만 권한을 주는 방식**을 권장합니다.

**참고**: `luckperms-setup.txt`를 처음 보내드렸을 때 `meta setdisplayname`이라는 잘못된 문법이 섞여 있어 "인식할 수 없는 명령입니다" 오류가 났었습니다 — 그룹 표시 이름은 `meta` 없이 `setdisplayname`으로, 프리픽스/서픽스(`meta setprefix`)는 우선순위 숫자를 첫 인자로 받도록 고쳐서 다시 보내드렸습니다. `yeowool.teleport.warp.manage`가 새로 추가돼서 스크립트에도 반영해 다시 전달드립니다 (관리진 그룹에 추가된 줄 하나만 실행하면 됩니다).

---

## 18. 최적화 & 테스트

- **아이템 복제 탐지 방식 변경**: 기존엔 30초마다 접속 중인 전원의 인벤토리+엔더상자를 무조건 전수 검사했는데, 이제는 **인벤토리 클릭/접속 시점에 즉시 검사**하는 방식으로 바꿨습니다. 전수 스캔은 이벤트로 못 잡는 경우(외부 플러그인이 이벤트 없이 직접 조작하는 경우 등)를 위한 안전망으로만 남기고 주기를 10분으로 늘렸습니다 (`anti-exploit.duplication-scan-interval-ticks`). 인원이 많아져도 부담이 커지지 않음
- **마을채팅 수신자 계산 정리**: 전체채팅 발송 시 매번 온라인 플레이어 목록을 복사하던 부분을 없애고 `Bukkit.getOnlinePlayers()`를 그대로 순회하도록 정리했습니다 (자잘한 할당 감소). 다만 실제로 살펴보니 마을채팅의 "메시지마다 접속자 전원과 토지 ID 비교" 자체는 인원이 아주 많지 않은 이상 체감될 정도로 무겁지 않아서, 토지ID→접속자 인덱스 같은 별도 캐시 구조까지는 만들지 않았습니다 (플러그인 간 이벤트 배선이 늘어나는 비용 대비 실익이 작다고 판단).
- **JUnit 5 테스트 인프라 추가**: 지금까지 자동화 테스트가 전무했던 부분을 보완. 서버 없이 검증 가능한 순수 로직 위주로 테스트를 작성했습니다:
  - `DurationParser`(기간 문자열 파싱), `CouponDateParser`(날짜 파싱) — 정상/경계/잘못된 입력값
  - `DurationFormat.humanize`(밀리초→한글 기간 텍스트), `TabCompletions.filter`(탭완성 필터링)
  - `PunishmentEntry`(만료/영구/해제 판정 로직)
  - `Land`(소유자/멤버 권한 판정, 토지 은행 잔액이 음수로 못 가는지)
  - `JobManager.requiredXpForLevel`(레벨업 경험치 곡선 공식)
  
  `./gradlew test`(또는 `build`에 포함)로 실행됩니다. Bukkit 서버 의존적인 나머지 로직(리스너, GUI, 커맨드 실행 등)은 여전히 테스트가 없고, 이번엔 손대지 않았습니다 — 서버를 띄우지 않고 안전하게 검증할 수 있는 범위만 우선 다뤘습니다.
- **`core.playerData().getOnline()`/`core.landStats()` 접속 직후 레이스 컨디션 전수 점검**: 사이드바/탭리스트에서 실제로 겪은 문제(플레이어 데이터 로드 전에 호출되어 예외가 나고, 주기 작업 전체가 그 틱에 멈춰버림)와 같은 패턴이 다른 곳에도 있는지 전체 코드베이스를 훑었습니다. 게임플레이 이벤트 리스너(채굴/낚시/농사 등)는 본인이 직접 트리거하는 이벤트라 이미 접속이 끝난 상태라 위험이 없고, 유일하게 같은 패턴이던 `TitleManager.checkUnlocks`(5분마다 접속자 전원의 칭호 자동달성 조건을 확인하는 `AchievementCheckTask`에서 호출)도 `getOnline()` → `getIfLoaded()`로 바꿔서 데이터가 아직 없으면 그 플레이어만 이번 주기에 건너뛰도록 고쳤고, `AchievementCheckTask`도 사이드바/탭리스트와 동일하게 플레이어별 try-catch를 추가했습니다.
- **설정 기본값 변경 마이그레이션 (`ConfigMerger.migrate`)**: `mergeDefaults`는 완전히 새로운 키만 채워주고, 이미 있는 키의 "기본값 문구"가 나중에 바뀐 경우(예: 탭리스트 헤더/푸터 문구, 사이드바의 `<player>`→`<identity>`)는 반영하지 못하는 한계가 있었습니다 (이번 세션에서 실제로 3번 부딪힘). `ConfigMerger.migrate` + `migrateScalarIfDefault`/`migrateListElementIfPresent`를 추가해서, 값이 "이전 기본값 그대로"일 때만 새 기본값으로 바꿔주는 방식으로 해결했습니다 — 운영자가 이미 직접 손댄 값(새 문구로 스스로 바꾼 경우 포함)은 절대 건드리지 않습니다. `YeowoolCommunity.migrateChangedDefaults()`에서 이번 세션에 있었던 3건의 변경을 등록해뒀고, 앞으로 비슷한 상황이 생기면 같은 방식으로 추가하면 됩니다.
- **사이드바 스코어보드 diff 렌더링**: 매 틱 objective를 통째로 unregister 후 재등록하던 것을 그만두고, 플레이어당 objective를 한 번만 등록한 뒤 제목/각 줄을 이전 틱에 렌더링한 `Component`와 비교해서 **실제로 바뀐 것만** 다시 보냅니다(Adventure `Component`는 구조적으로 `equals` 비교가 되므로 비교 자체는 저렴함). 접속 종료 시 캐시(마지막으로 렌더링한 제목/줄)도 정리해서 메모리가 계속 쌓이지 않도록 함
- **랭크아이콘/칭호 렌더링 캐싱** (`PlayerIdentityService`): 아이콘(ItemsAdder 호출)+칭호(MiniMessage 파싱) 부분("prefix")을 접속/변경 시점에만 계산해서 캐싱하고, 매 틱 사이드바/탭리스트가 다시 그릴 때는 캐시된 값을 그대로 재사용하도록 바꿨습니다. 닉네임/실명 부분은 원래도 단순 설정값 조회라 그대로 매번 계산해도 무리 없어서 캐싱하지 않았습니다. 접속 종료 시 캐시 삭제
- **오프라인 대상 지원** (`PlayerDataResolver`, yeowool-core): `/랭크아이콘 부여|제거`, `/칭호 지급|회수`, `/한글닉네임설정권`이 전부 온라인 플레이어만 대상 가능했던 제약을 없앴습니다. 대상이 온라인이면 기존처럼 캐시에서 바로 처리하고, 오프라인이면(이 서버에 접속한 적 있는 경우) DB에서 잠깐 로드해서 처리한 뒤 저장하고 다시 캐시에서 내립니다. 한글닉네임설정권은 아이템이라 오프라인 대상에게 직접 인벤토리로 줄 수 없어서, `/여울관리 우편`과 같은 우편함(`MailboxService.deliverOrStore`)으로 전달하도록 함
- **매크로 탐지 임계치 / DB 커넥션 풀 크기 등은 이번엔 손대지 않았습니다** — 실사용 트래픽 데이터 없이 임의로 숫자를 바꾸는 건 근거 없는 튜닝이라 판단해서, 실제 운영하면서 오탐/미탐 비율이나 커넥션 풀 포화 여부를 보고 조정하는 걸 권장합니다.
